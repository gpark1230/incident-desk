DATABASE_URL=postgresql://incident_desk_user:devpassword123@localhost:5432/incident_desk
SECRET_KEY=changeme

# Optional -- defaults to redis://localhost:6379/0 if unset. Incident events
# (create/update/comment) publish here if reachable; the app works fine
# without Redis running at all.
REDIS_URL=redis://localhost:6379/0
